/** Shared styling for form inputs/selects so every control matches. */
export const controlClass =
  'rounded-lg border border-bark/40 bg-paper px-3 py-2 font-body text-ink shadow-inner focus:border-robin focus:outline-none'

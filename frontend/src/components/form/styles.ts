/** Shared styling for form inputs/selects so every control matches. */
export const controlClass =
  'rounded-lg border border-bark/40 bg-parchment px-3 py-2 font-body text-ink focus:border-robin focus:outline-none'

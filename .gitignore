# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Claude Code local settings (per-machine; not shared)
.claude/settings.local.json

# BirdNET-Go binaries & libraries (installed under ~/birdnet-go, not in-repo;
# ignore here in case build/download artifacts land in the working tree)
birdnet-go
*.tar.gz
*.dylib
*.so
*.dll

# BirdNET-Go local data, models, logs & config
data/
cache/
posters/
models/
logs/
*.log
config.yaml
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3
clips/
recordings/
*.wav
*.mp3
*.flac

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Frontend (node)
node_modules/
*.tsbuildinfo

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Environment
.env
.env.local

# Samsung TV auth token (paired per-device; not shared)
.tv-token*
